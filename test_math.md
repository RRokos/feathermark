# Math Test

This formula has `<` and `>` inside inline math:

When $f'(x) > 0$: $1 - x^2 > 0$ → $-1 < x < 1$, increasing.

Block math:

$$f'(x) = \frac{1 - x^2}{(1 + x^2)^2}$$

The inequality $a < b < c$ should display correctly.
